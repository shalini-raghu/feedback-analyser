[project]
name = "feedback-analyser"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Shalini", email = "shalinihraghu@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [ "fastapi[standard]>=0.136.3",
    "google-genai>=2.8.0",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
    "streamlit>=1.58.0",
]

[project.scripts]
feedback-analyser = "feedback_analyser:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
